# PatientRegistrationApp(work in progress)
Console application designated to register patients for appointments
Patients as instances of Patient object are (going to be) stored inside the dictionary 
